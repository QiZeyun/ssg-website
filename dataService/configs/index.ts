/**
 * 配置数据源模块
 * 
 * 提供统一的抽象接口，用于从外部系统（如 CMS、API、文件系统等）获取配置信息
 * 
 * 此模块封装了所有配置数据源的实现细节，调用方只需要通过接口获取配置数据，
 * 而不需要感知数据的具体来源（文件、CMS、API 等）
 */

// 导出 SEO 配置数据源
export * from './seo';

// 导出价格配置数据源
export * from './pricing';

// 导出内容数据源
export * from './content';
